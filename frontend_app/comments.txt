#1 First Start on the Docker

docker-compose up

#2 For Build flask_app on the Docker

docker-compose build

#2 Second Start flask_app on the Docker

docker-compose up -d



# for gitub
#-1     git remote add upstream https://github.com/UnilabEdu/UnilabPythonInternship.git
#-2     git fetch upstream
#-3     git pull upstream master
#-4     git merge upstream/master


sudo nano /etc/systemd/system/uwsgi_frontend.service

sudo systemctl daemon-reload  # Reload Systemd
sudo systemctl enable uwsgi_frontend  # Enable it to start on boot
sudo systemctl start uwsgi_frontend  # Start the uWSGI service


sudo chown -R iesflask:www-data /var/www/frontend_app/
sudo chmod -R 755 /var/www/frontend_app/

