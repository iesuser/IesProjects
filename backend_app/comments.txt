#1
python3 -m venv env

#2
source env/bin/activate

#3
pip3 install -r requirements.txt

#4
for server start
python3 app.py

# for Database
flask init_db
flask populate_db

swagger.py -ს აქვს პრობლემა სურათის ატვირთვის დროს 
venv/lib/python3.10/site-packages/flask_restx/swagger.py -ში
469 -ე ახზზე :  if any(p["type"] == "file" for p in all_params):  უნდა ჩანაცვლდეს:
if any(p["type"] == "file" or (p["type"] == "array" and p["items"]["type"]=="file") for p in all_params):



# on server
sudo chown -R $USER:$USER /var/www/backend_app/venv
sudo apt-get update && apt-get -y install python3-dev gcc build-essential
#1 
sudo apt install python3.10-venv
#2
source env/bin/activate
sudo pip install --upgrade pip setuptools wheel
pip3 install -r requirements.txt




sudo systemctl daemon-reload  # Reload Systemd to recognize the new service
sudo systemctl enable uwsgi_backend  # Enable it to start on boot
sudo systemctl start uwsgi_backend  # Start the uWSGI service
sudo systemctl status uwsgi_backend
